# Dependencies
node_modules/

# Build output
dist/
build/
out/
.next/
.nuxt/
.output/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Coverage
coverage/
.nyc_output/

# Workbench Phase 1 PoC artifacts
.playwright-workbench/

# Smoke spec artifacts (run output, captured screenshots)
e2e/_artifacts/
e2e/test-results/
e2e/playwright-report/

# Design iteration scratch (Playwright MCP snapshots, iteration screenshots)
# モックアップを更新する際の作業ファイルはここに置く: docs/design/_scratch/
.playwright-mcp/
docs/design/_scratch/

# Autonomous-loop session state (per-session, not committed)
.PLAN_v2_AUTORUN.lock
PROGRESS.md
.agents/state/

# Claude Code / Codex per-machine session state
# - settings.local.json: user-local override (do not commit; see .claude/settings.json for the project-level config)
# - scheduled_tasks.lock: per-session lock from /schedule
# - sessions / projects: session transcripts
# - worktrees: per-session worktree scratch
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/projects/
.claude/sessions/
.claude/worktrees/

# Codex per-machine session state (the .codex/ directory committed here is
# project-level config and hooks only)
.codex/sessions/
.codex/cache/
.codex/log/
