# Claude Code entry point

Project context lives in [`AGENTS.md`](AGENTS.md). Read that first.

Tool-specific configuration: [`.claude/settings.json`](.claude/settings.json).
Skills: [`.agents/skills/`](.agents/skills/) (Agent Skills open standard); also exposed via [`.claude/skills/`](.claude/skills/) for direct discovery.
Rules: [`.agents/rules/`](.agents/rules/).

The cross-tool source of truth is `AGENTS.md`. Do not duplicate guidance here.
